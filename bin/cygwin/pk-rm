#!/bin/bash

. pk-common

name="$1"

pk-create-local-dirs

[ -z "$name" ] && { echo "ERROR: package not specified"; exit 1; }

echo "remove $name"

list_file=`readlink -f "list/$name.list"`

cd libs
rm `cat "$list_file"`
cd ..
rm "$list_file"
rm -r "packages/$name"
