#!/bin/bash

set -e

. pk-common

[ -d "$BACKUP_PATH" ] || { echo "Backup path $BACKUP_PATH is not reachable"; exit 1; }

rsync_options='-av'
rsync "$rsync_options" "$INSTALL_PATH" "$BACKUP_PATH"
rsync "$rsync_options" "$SRC_PATH" "$BACKUP_PATH"
